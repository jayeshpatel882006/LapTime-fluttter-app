class AppConstants {
  AppConstants._();

  static const String appName = 'LapTime';
  static const String timerTitle = 'LapTime';

  static const Duration timerInterval = Duration(milliseconds: 10);
  static const Duration animationDuration = Duration(milliseconds: 350);
  static const Duration shortAnimationDuration = Duration(milliseconds: 200);
  static const Duration longAnimationDuration = Duration(milliseconds: 500);

  static const double buttonSize = 88.0;
  static const double buttonIconSize = 36.0;
  static const double timerFontSize = 80.0;
  static const double lapCardHeight = 80.0;
  static const double borderRadius = 28.0;
  static const double cardBorderRadius = 20.0;
  static const double defaultPadding = 24.0;
  static const double smallPadding = 12.0;
  static const double largePadding = 32.0;
  static const double buttonSpacing = 40.0;

  static const String zeroPadFormat = '00';
  static const String zeroPadFormatMs = '000';
}
